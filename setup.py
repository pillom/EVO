from setuptools import find_packages, setup

setup(
    name="evoinfer",
    version="0.1.0",
    description="EVO cache scheduling for transformer-based Diffusion Policy inference.",
    packages=find_packages(),
)
